# Copy to .env and fill in values.
# HF_TOKEN is only required for gated models (UNI, UNI2-h, Virchow, Virchow2).
# Leave blank or omit for the default phikon-v2 / hibou-b encoders.
HF_TOKEN=

# Optional: override device (auto-detected from config.yaml by default)
# DEVICE=cuda
